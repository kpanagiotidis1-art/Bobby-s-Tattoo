# Supabase project settings (Project Settings > API in the Supabase dashboard)
# Safe to expose in the browser: protected by Row Level Security, not secret keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
